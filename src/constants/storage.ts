/**
 * Storage and cookie keys for persistent auth and preferences.
 */
export const STORAGE_KEYS = {
  ACCESS_TOKEN: "omni_access_token",
  REFRESH_TOKEN: "omni_refresh_token",
  USER: "omni_user",
} as const;
